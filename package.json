{
  "name": "recepcionista",
  "version": "0.0.0",
  "private": true,
  "description": "Agente de voz que agenda contra a API do AgendaFacil",
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "tipos": "tsc --noEmit",
    "teste": "node --test",
    "verificar": "npm run tipos && npm run teste",
    "contrato": "node --test tests/contrato.test.ts",
    "medir": "node scripts/medir-ferramentas.ts",
    "conversa": "node --env-file=.env src/cli.ts",
    "avaliar": "node scripts/avaliar.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0"
  }
}
